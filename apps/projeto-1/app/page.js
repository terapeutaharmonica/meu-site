export default function Home() {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        gap: "0.5rem",
        textAlign: "center",
        padding: "2rem",
      }}
    >
      <h1>Projeto 1</h1>
      <p style={{ color: "#666" }}>
        Placeholder de teste — nome, conteúdo e escopo ainda serão definidos.
      </p>
    </main>
  );
}
